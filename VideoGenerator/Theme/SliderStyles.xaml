<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Sleek PC-style Slider for high-end professional HUD configuration -->
    <Style x:Key="ModernSlider" TargetType="Slider">
        <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="IsMoveToPointEnabled" Value="True"/>
        <Setter Property="Height" Value="28"/>
        <Setter Property="Margin" Value="0,8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid Background="Transparent" SnapsToDevicePixels="True">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Track Background -->
                        <Border x:Name="TrackBackground" 
                                Grid.Row="1" 
                                Height="4" 
                                Background="#2A2A30" 
                                CornerRadius="2" 
                                VerticalAlignment="Center" 
                                Margin="4,0">
                            <Canvas Margin="-4,-1,-4,-1">
                                <Rectangle x:Name="PART_SelectionRange" Fill="{StaticResource AccentBrush}" Height="4" Visibility="Collapsed"/>
                            </Canvas>
                        </Border>

                        <Track x:Name="PART_Track" Grid.Row="1" VerticalAlignment="Center">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Focusable="false">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Height="4" Background="{StaticResource AccentBrush}" CornerRadius="2" Margin="4,0,0,0"/>
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Focusable="false">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Height="4" Background="Transparent"/>
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb x:Name="Thumb" Focusable="false" Width="12" Height="12" UseLayoutRounding="True">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Ellipse x:Name="ThumbEllipse" 
                                                     Fill="#E4E4E7" 
                                                     Stroke="#18181B" 
                                                     StrokeThickness="1.5"
                                                     Width="12" 
                                                     Height="12" 
                                                     HorizontalAlignment="Center" 
                                                     VerticalAlignment="Center"/>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="true">
                                                    <Setter TargetName="ThumbEllipse" Property="Fill" Value="#FFFFFF"/>
                                                    <Setter TargetName="ThumbEllipse" Property="Stroke" Value="{StaticResource AccentBrush}"/>
                                                    <Setter TargetName="ThumbEllipse" Property="StrokeThickness" Value="2"/>
                                                </Trigger>
                                                <Trigger Property="IsDragging" Value="true">
                                                    <Setter TargetName="ThumbEllipse" Property="Fill" Value="{StaticResource AccentBrushLight}"/>
                                                    <Setter TargetName="ThumbEllipse" Property="Stroke" Value="{StaticResource AccentBrush}"/>
                                                    <Setter TargetName="ThumbEllipse" Property="StrokeThickness" Value="2"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="TrackBackground" Property="Background" Value="#15FFFFFF"/>
                            <Setter TargetName="Thumb" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Global implicit style to automatically apply the ModernSlider theme everywhere -->
    <Style TargetType="Slider" BasedOn="{StaticResource ModernSlider}"/>

</ResourceDictionary>
