<UserControl x:Class="VideoGenerator.Views.TranslationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid Margin="32,8,32,8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Dictionary &amp; Events" 
                   Style="{StaticResource MaterialDesignHeadline4TextBlock}" 
                   Margin="0,24,0,16" 
                   FontWeight="Bold"/>
                   
        <TextBlock Grid.Row="1" 
                   Text="Edit the raw JSON to add support for new champions, skins, and specific event texts. Be careful to maintain valid JSON syntax." 
                   Style="{StaticResource MaterialDesignSubtitle1TextBlock}" 
                   Opacity="0.7"
                   Margin="0,0,0,24" />

        <materialDesign:Card Grid.Row="2" Style="{StaticResource ModernCard}" Padding="0">
            <TextBox Text="{Binding JsonContent, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     FontFamily="Consolas"
                     FontSize="13"
                     Foreground="#E0E0E0"
                     Background="#0D0D0D"
                     BorderThickness="0"
                     Padding="16"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"/>
        </materialDesign:Card>

        <Grid Grid.Row="3" Margin="0,24,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding StatusMessage}" 
                       VerticalAlignment="Center" 
                       Foreground="{StaticResource AccentBrush}" 
                       FontWeight="Bold"/>
            <Button Grid.Column="1" 
                    Content="SAVE TRANSLATIONS" 
                    Click="Save_Click"
                    Style="{StaticResource ModernActionButton}"
                    Width="200"
                    Margin="0"/>
        </Grid>
    </Grid>
</UserControl>