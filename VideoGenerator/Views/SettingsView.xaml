<UserControl x:Class="VideoGenerator.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <materialDesign:PackIcon Kind="Cog" Width="64" Height="64" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center"/>
            <TextBlock Text="Settings Section" Style="{StaticResource MaterialDesignHeadline4TextBlock}" Margin="0,16" HorizontalAlignment="Center"/>
            <TextBlock Text="Advanced configuration options will appear here." Opacity="0.6" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
